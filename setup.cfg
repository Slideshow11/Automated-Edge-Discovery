[metadata]
name = automated_edge_discovery
version = 0.1.0

[options]
packages = find:
include_package_data = True
install_requires =
    # Keep minimal; tests may require pandas which is installed in test environment

[options.packages.find]
where = .
exclude = tests
